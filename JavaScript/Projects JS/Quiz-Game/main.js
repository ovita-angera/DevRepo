// get some console input from our users
// install a package that can help with the collection of input
const prompt = require("prompt-sync")()

const value = prompt("Enter your name: ")
console.log(value);